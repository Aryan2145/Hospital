You are working inside an existing Hospital CRM codebase (VIROC Hospital CRM).
This is an UPGRADE to the current product, not a rebuild.

GOAL:
Implement the Desktop wireframe changes for:
1) Leads Workspace (list view)
2) Lead Detail page
3) Episode Detail page (tabs + panels)

IMPORTANT CONSTRAINTS:
- Do NOT change navigation/module placement.
- Do NOT break existing flows (lead → appointment → episode).
- Keep design simple for Indian users (fast scanning, minimal fields).
- All changes must be additive or non-breaking.

NON-NEGOTIABLE PROCESS (MUST FOLLOW):
1) AUDIT what exists now (UI components, routes, DB schema, enums, permissions).
2) PROPOSE a detailed plan (files to change, schema updates if any, UI changes).
3) STOP and ASK for approval: “Approve Plan? Yes/No”.
4) Only after approval, implement changes in small commits.

========================================================
A) DESKTOP WIREFRAME TARGET (WHAT TO BUILD)

1) LEADS WORKSPACE (DESKTOP LIST VIEW)
Current columns likely: Name, Contact, Source, Status, Priority, Next Action, Created.

Update list view to show these columns (desktop):
- Name
- Stage (operational stage: Raw Lead / Appointment Booked / Checked In / Consultation Done / etc.)
- Temperature (badge: Cold/Warm/Warm+/Warm++/Hot/Very Hot/Dormant)
- Owner (current team + user, derived from auto-handover)
- Ageing (time since lead created OR last activity; show compact e.g., 3h/1d; red if SLA breached)
- Next Action (task summary; highlight overdue)
- Source (Meta/Callyzer/Manual etc.) (optional if space)

Remove “Priority” from list (temperature replaces it).

Add top quick filters (desktop):
- All
- My Leads
- Hot
- Dormant
- Overdue
- Telecalling / Front Office / Doctor / Insurance (team filters)
Keep these as buttons or a filter bar; do not add complex filter UI.

2) LEAD DETAIL PAGE (DESKTOP)
Header must show badges:
[Stage] [Temperature] [Owner Team] [Source]
Example: Appointment Booked | Hot | Insurance | Meta

Add a compact “Intelligence Strip” below header:
Lead Age | Reschedules | No Shows | FRT (first response time) | Episodes Count | Probability%
Example: 1d | 1 | 0 | 7m | 1 | 70%

Main desktop layout (2 columns):
LEFT (70%):
- Episodes section (list linked episodes + status badges)
- Activity Timeline

RIGHT (30%):
- Next Action card
- Tasks card
- Current Ownership card (team, primary owner, last handover)
- Temperature History card (latest transitions)

3) EPISODE DETAIL PAGE (DESKTOP)
Keep existing clinical journey stages unchanged.

Add top horizontal tabs:
Clinical Status | Financial Status | Insurance Status | Family Status

Tab behavior:
- Default to Clinical Status
- Switch tabs without page reload (client-side)
- Preserve episode_id context and permissions per tab

Clinical Status tab:
- Existing treatment journey + clinical notes (no major change)

Financial Status tab (add fields + discount):
- estimate_amount
- estimate_shared (bool + datetime)
- negotiation_status (None / In Discussion / Finalized)
- discount_applied (bool)
- discount_type (Flat / Percentage)
- discount_value
- discount_approved_by
- discount_approved_at
- final_estimated_amount (system-calculated)
- advance_received_amount
- payment_mode

Insurance Status tab (STRICT DROPDOWNS ONLY):
- insurance_applicable (bool)
- policy_type_id (dropdown)
- insurer_id (dropdown)
- tpa_id (dropdown)
- preauth_status_id (dropdown)
- preauth_submitted_at
- preauth_approved_amount
- rejection_reason_id (dropdown)

IMPORTANT GOVERNANCE:
- No free text allowed in Insurance tab.
- If user needs new Insurer/TPA, provide “Request New” option that creates a master request.
- Episode cannot use the new master until approved. After approval, it appears in dropdown.

Family Status tab:
- family_discussion_done
- second_opinion_taken
- decision_status (Pending / Likely / Confirmed / Dropped)
- decision_notes (optional)

========================================================
B) REQUIRED LOGIC/INTEGRATIONS (DON’T SKIP)

1) Lead Stage and Owner Display
- Lead Stage should be derived from lead + appointment + episode signals.
- Owner should reflect auto-handover’s current owner_team and primary_owner_user_id.

2) Temperature + History
- Ensure temperature badge uses existing temperature engine.
- Temperature history card should show last 3 transitions with reason & timestamp.

3) SLA / Ageing
- Ageing should be displayed as:
  - “time since created” OR “time since last_activity_at”
Choose best based on current DB fields; propose in plan.
- SLA breach should display red indicator (do not implement complex SLA rules unless already present).

4) Episodes Section on Lead Detail
- Show episodes linked to lead/patient.
- Each episode row should show key badges:
  - Clinical stage badge (e.g., Consultation Done)
  - Optional insurance badge if approved/rejected
- Clicking an episode opens the episode detail page.

========================================================
C) AUDIT FIRST (OUTPUT REQUIRED BEFORE ANY CODE CHANGE)

Before doing anything, inspect and report:
- Current routes/components for:
  - Leads list page
  - Lead detail page
  - Episode detail page
- Existing DB fields for lead temperature, ownership, reschedule/no-show counts, etc.
- How appointments and episodes are linked to leads/patients currently
- Existing UI library/components for tabs, badges, cards

Then propose:
- EXACT files to modify
- Any schema changes needed (if fields missing)
- Minimal migration plan (non-breaking)

STOP and ask:
“Approve Plan? Yes/No”

========================================================
D) IMPLEMENTATION (ONLY AFTER APPROVAL)

After approval:
- Implement leads list columns + filter bar
- Implement lead detail 2-column layout + intelligence strip + ownership card + temperature history
- Implement episode tabs and new tab content
- Add master request flow hooks for insurer/TPA if not already present
- Add QA checklist and test steps

QA must include:
- Existing lead flow still works
- Lead list loads fast and responsive
- Permissions respected (doctor sees assigned patients after check-in)
- Insurance tab enforces dropdown-only and master approval request
- No regressions in appointments and episode creation flow