We have now created the Meta Integration Help Document inside MyProSys Hospital CRM. Before we freeze this and move forward, I want you to do a final QA validation and alignment check across both:

1. Actual implementation (backend + connector logic)
2. Help documentation (user-facing)

This is NOT a redesign request. This is a validation + correction pass.

You already understand:
- Multi-tenant architecture
- Leads Workspace
- Dashboards / Insights
- Connector structure
- Existing schema and flows

Your task is to:

A. Validate implementation vs documentation
B. Highlight any mismatches
C. Fix the help document wherever required
D. Suggest minimal corrections in system (only if critical)

--------------------------------------------------

🔴 PART 1: CRITICAL ARCHITECTURE VALIDATION

1. Meta App Ownership Model

Check and confirm:

- Is MyProSys using ONE central Meta Developer App (recommended SaaS model)?
OR
- Is EACH hospital required to create its OWN Meta Developer App?

Then:

- If central app → update help doc to REMOVE "create your own app"
- If per-tenant app → confirm this is intentional and justified

👉 Output:
Clear statement of current architecture + whether it is correct

--------------------------------------------------

2. Permissions Validation

Currently documented permissions:

- ads_read
- ads_management
- leads_read
- pages_read_engagement
- pages_manage_ads

Validate against actual implementation:

- Are these EXACT permissions used in token generation?
- Should it be leads_read OR leads_retrieval?
- Is pages_manage_metadata required for webhook subscription?
- Any missing permission required for:
  - lead fetching
  - insights pulling
  - page linkage

👉 Output:
Final correct permission list (production-ready)

👉 Also:
Update help document to reflect exact correct permissions ONLY

--------------------------------------------------

3. Lead Capture Mechanism (VERY IMPORTANT)

Validate actual system behavior:

- Are we using:
  a) Webhooks (real-time push from Meta)
  OR
  b) Polling API (periodic fetch using leadgen_id)
  OR
  c) Hybrid

Also validate:

- Is webhook subscription handled internally by MyProSys?
- OR does the hospital admin need to configure webhook manually?

👉 Output:
- Exact mechanism used
- Whether any user action is required

👉 Then:
Fix help document to remove ambiguity:
- If automatic → remove manual webhook instructions
- If manual → make steps very explicit and clear

--------------------------------------------------

4. Page / Asset / Form Mapping Validation

Currently document focuses heavily on:
- Ad Account ID

But validate:

- How does system know WHICH:
  - Facebook Page
  - Instagram Account
  - Lead Forms

are connected?

Check:

- Is Page selection part of connection flow?
- Is it inferred from Ad Account?
- Is there any UI to select / verify Page?
- Are ALL forms auto-captured or filtered?

👉 Output:
Clear explanation of actual logic

👉 Then:
Update help document to clearly explain:
- What exactly is connected
- Whether user needs to select anything

--------------------------------------------------

5. Insights Sync Mechanism

Validate:

- Is insights fetching:
  - automatic (cron)
  - manual (Sync button)
  - both

- What is frequency?

👉 Then:
Ensure help document clearly states:
- real behavior
- expected delay
- user action if required

--------------------------------------------------

🟠 PART 2: HELP DOCUMENT CORRECTIONS

Now review the existing help document and fix the following:

1. Remove any developer-heavy or misleading instructions
2. Align steps EXACTLY with actual product UI
3. Ensure no step requires user action that system already handles internally
4. Ensure no missing step that is actually required

--------------------------------------------------

🟡 PART 3: UX + PRODUCT CORRECTIONS (ONLY IF CRITICAL)

Only suggest changes if HIGH IMPACT:

Examples:
- Missing Page selection causing lead failure
- Missing permission causing silent failure
- Token flow likely to break in production
- Confusing onboarding steps

Do NOT suggest cosmetic or optional improvements.

--------------------------------------------------

🟢 PART 4: FAQ COMPLETION

Currently FAQ section has questions but no answers.

You must:

- Write clear, practical answers for each question
- Keep answers short, real-world, and user-friendly

--------------------------------------------------

🔵 PART 5: FINAL OUTPUT FORMAT

Provide output in 4 sections:

1. ✅ Architecture Summary
- How system currently works (Meta App, token, webhook, sync)

2. ⚠️ Issues / Gaps Found (if any)
- Only critical mismatches

3. ✏️ Help Document Corrections
- Only the corrected sections (not full rewrite)

4. 📋 Final Validated Permission List

--------------------------------------------------

IMPORTANT CONSTRAINTS

- Do not redesign the system
- Do not create theoretical answers
- Base everything on actual implemented logic
- Keep output crisp, structured, and implementation-oriented
- Assume this will directly impact production onboarding

--------------------------------------------------

FINAL GOAL

Ensure that:

👉 What the help document says = exactly how the system works  
👉 A hospital admin can connect Meta successfully without confusion  
👉 No silent failures happen due to wrong permissions or missing steps  
👉 Multi-tenant isolation remains intact  
