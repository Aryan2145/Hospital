AUDIT FIRST, THEN DESIGN (Approval-Gated)
Project: Hospital CRM (NOT HMS)

IMPORTANT ROLE LANGUAGE:
- Super Admin = Programmer team (Replit / system-level override only)
- CRM Admin = Hospital frontend operational admin

CRITICAL INSTRUCTION:
DO NOT implement or change any workflow yet.
Step 1 is ONLY to explain/document the CURRENT workflow as it exists in the system today.
Only after I (user) approve the documented current workflow, proceed to Step 2 (design improvements).

========================================================
STEP 1: DOCUMENT CURRENT WORKFLOW (AS-IS) — NO CHANGES
========================================================

A) Identify where Episode stage is stored and updated:
- DB fields (episode_stage etc.)
- API endpoint(s) that update stage
- UI actions (clickable pills? dropdown? button?)

B) Explain how transitions happen right now (exact behavior):
For each stage:
Consultation Done
Treatment Planning
Surgery Scheduled
Surgery Done
In Treatment
Post Care
Follow Up
Completed

Document in a table:
- Current Stage
- How user moves to next stage currently (UI action)
- Which API endpoint is called
- Is there any validation now? (Yes/No + what)
- Who can do it currently (role list as per code)
- What logs are created (if any)
- What tasks/appointments are created automatically today (if any)

C) Document the current stakeholder role model used in code:
List roles found in code/config for episode edits and stage changes.
Map them to:
- Doctor
- Medical Assistant
- Front Office
- Counsellor
- CRM Admin
- Super Admin

D) Document current “drop” handling (if any):
- Is there currently any status like Drop/Lost/Closed?
- If yes: where stored, how triggered, who can mark, what reason captured
- If no: clearly state “No current drop capture exists”

Deliverable of Step 1:
- “AS-IS Episode Journey Workflow Report”
- Include screenshots/field names/endpoints referenced
- NO suggestions yet, NO new design yet.

WAIT for my approval after Step 1.

========================================================
STEP 2: ONLY AFTER MY APPROVAL — DESIGN IMPROVEMENTS
========================================================

When I approve Step 1, propose a CRM-suitable improved model that includes:

A) “DROP at Any Stage” (CRM Outcome Layer)
Because this is CRM (not HMS), drop means:
- Patient did not proceed further in this journey (for any reason)
- We still need follow-up/revival possibilities

Implement as an outcome layer without breaking journey:
- Add fields:
  - episode_outcome_status: ACTIVE | DROPPED | COMPLETED
  - dropped_at_stage (stores which stage drop happened)
  - dropped_reason_category (picklist)
  - dropped_reason_notes (text)
  - dropped_by (user)
  - dropped_at (datetime)
  - next_revival_date (optional)
  - revival_status (OPEN / IN_PROGRESS / CLOSED)

B) Multiple Stakeholders Can Move Stages
Design a permission matrix (CRM-friendly):
- Stage transitions should be allowed for:
  - Doctors
  - Medical Assistants
  - Front Office
  - Counsellors
  - CRM Admin
(And Super Admin only for override/backward/system)

Provide a “Transition Permission Matrix” table:
- Transition (From → To)
- Allowed Roles
- Prerequisites checklist
- Trigger type (Manual/Auto/Hybrid)

C) CRM-Based Triggers (not HMS)
Avoid dependencies on OT/IP/HMS data.
Use CRM actions as triggers:
- appointment booked
- note saved
- estimate shared
- consent captured (as a CRM checkbox/doc upload)
- surgery date captured (planning)
- discharge summary uploaded (if available)
- post care tasks scheduled
- follow-up booked

D) Post Care: Home Visits + Hospital Visits
Within Post Care stage:
- allow creating home visit tasks for physio/caregivers
- allow booking hospital visit appointments with departments
- ensure these tasks/appointments link to episode_id and show in episode timeline

E) Approval Gate
After proposing Step 2 design, DO NOT implement until user confirms approval again.
Provide “Changes to implement” checklist for dev after approval.

Deliverables of Step 2 (after approval):
- Proposed state machine + outcome layer (Active/Drop/Complete)
- Validation rules
- Role permissions matrix
- API payload samples
- DB minimal changes required
- UI behavior changes (Move Next + Drop button + Revival reminders)

========================================================
END
========================================================
First complete Step 1 only. Then stop and ask for my approval before Step 2.